/**
 * This code is only executed when jQuery determines that the page is ready.
 * This is essential as you don't want to be interacting with the page with
 * javascript until then.
 */
$(document).ready(function() {
    // Write your javascript here
});
